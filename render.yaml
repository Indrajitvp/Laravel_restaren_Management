services:
  - type: web
    name: laravel-restaurant-app
    env: php
    region: singapore
    plan: free
    buildCommand: "composer install && php artisan migrate --force"
    startCommand: "php artisan serve --host 0.0.0.0 --port $PORT"
